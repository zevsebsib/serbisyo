import 'package:flutter/material.dart';
class AdminCitizensScreen extends StatelessWidget {
  const AdminCitizensScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Citizens'));
}